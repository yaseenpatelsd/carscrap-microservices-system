server:
  port: 8761

spring:
  application:
    name: service-registry

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    lease-renewal-interval-in-seconds: 5appointment
    lease-expiration-duration-in-seconds: 10
